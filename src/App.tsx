import { Button } from '@/components/ui/button'

function App() {
  return (
    <div className="flex min-h-svh flex-col items-center justify-center gap-4">
      <h1 className="text-2xl font-semibold">My Shop Admin</h1>
      <p className="text-muted-foreground">Scaffold ready — routing and layout coming next.</p>
      <Button>shadcn/ui is wired up</Button>
    </div>
  )
}

export default App
